# TripSupporter change-log

## 2026-04-28

- 空の作業ディレクトリに Vite + React のウェブアプリ構成を追加。
- 受領した観光ルート作成アプリを `src/App.jsx` として配置。
- Tailwind CSS と lucide-react を利用できる構成を追加。
- Google Maps API キーと GAS URL を Vite 環境変数から読むように変更。
- ローカル開発時の CORS 回避用に Vite の `/gas` proxy を追加。
- 起動手順と現在の GAS 側エラーを `README.md` に記録。
- Apps Script に貼り付ける安全版コードを `gas/Code.gs` として保存。
- `.env.local` の GAS URL を新しいウェブアプリ URL に更新。
- ログイン後に Google Maps の地図DOMが表示された時点で初期化されるよう修正。
- ログアウト・管理者画面遷移後に地図インスタンスをリセットするよう修正。
- Google Maps APIキー拒否時のエラーメッセージを追加。
- 管理者画面で各班のPINを確認できる表示を追加。
- `/gas` を通常proxyから、GASリダイレクトを追跡する開発用中継に変更。
- 生徒ログインの班番号を一時的に自由入力式へ変更。
- PINの比較を文字列同士にそろえ、スプレッドシート側が数値でもログインできるよう修正。
- GASが `getActiveSheet()` に依存して別タブへ書き込まないよう、指定スプレッドシートIDと `gid=49856829` のタブを明示して読む・書く形に修正。
- 管理者PINを `VITE_ADMIN_PIN` で管理できるようにし、READMEにログイン方法を明記。
- `.env.local` の GAS URL を新しいデプロイ URL に更新。
- `.env.local` の GAS URL を再デプロイ後の新しい URL に更新。
- `.env.local` の GAS URL を最新デプロイ URL に更新。
- 誤って別シート由来の `5101` 形式へ寄せた班番号入力を戻し、正仕様の `01` から `12` の選択式に修正。
- `.env.local` の Google Maps APIキーを新しいキーへ更新。
- 地図上に出発地・目的地の番号付きマーカーを表示するよう修正。
- 複数目的地を区間ごとの経路線で結ぶよう修正。
- 「見積り」ボタンでGoogle Mapsの経路時間を移動時間へ反映するよう修正。
- Google Placesの写真を行程カード右側にプレビュー表示し、タップで拡大できるよう追加。
- 各地点を赤い円で強調し、入力地点範囲へ地図が自動追尾するよう修正。
- 経路線を区間ごとに色分けし、行程表の移動手段チップにも対応色を表示。
- 地点強調用に赤いリング型マーカーを追加し、最後に追加した地点へパンするよう調整。
- 見積り時に公共交通の乗車場所・降車場所をGoogleの経路結果から自動補完するよう追加。
- 地図の追尾を表示中の全地点が入る範囲へ戻し、地点ごとの地図表示チェックボックスを追加。
- 地点・乗降場所の入力候補表示を追加し、見積り時は入力済みの乗車場所・降車場所を優先するよう修正。
- 京都・奈良限定モードのチェックボックスを追加し、候補検索・地点検索を京都奈良周辺へ寄せるよう修正。
- 乗車場所・降車場所候補を公共交通の駅・停留所優先に変更。
- 公共交通Directionsが返らない場合の道路経路ベース概算を廃止し、手入力を促す表示に戻した。
- 見積りエラー文から車代用に関する説明を削除し、電車/バス見積り時にGoogle Mapsの公共交通モードを明示するよう修正。
- ボタン表示を `見積り(徒歩)` に変更。
- 通過地点を扱えるよう、滞在時間0分の入力を許可。
- 候補選択時に住所全文ではなく短い主名称を入力欄へ入れるよう修正。
- 仕様正本、ルールブック、回帰チェックリスト、変更履歴を追加。
- 乗車場所・降車場所欄を削除し、駅名や乗換地点も目的地として追加する運用へ整理。
- 電車・バス選択時に、所要時間は検索するよう赤字太字の注意を移動手段欄へ表示。
- 行程表の地点名にも、地図マーカーと同じ番号を表示。
- 候補選択時の `placeId` を保存し、手入力地点が明確に特定できない場合は地図表示をスキップするよう修正。
- GASに管理者用タイムライン出力処理を追加し、`gid=1348031489` のタブへ日別・時刻別・班別の所在地表を作成できるよう修正。
- 管理者画面に「タイムライン出力」ボタンを追加。
- ローカル開発用 `/gas` 中継でクエリ文字列をGASへ渡すよう修正。
- 管理者用タイムラインを、班を縦軸・時刻を横軸にしたガントチャート型へ変更。滞在場所は横棒、昼食・カフェ等はオレンジ、通常地点は緑で表示。
- タイムライン出力中の表示を追加し、GASのWebアプリが古いデプロイを実行している場合に具体的なエラーを表示するよう修正。
- `.env.local` の GAS URL を新しいWebアプリURLへ更新。
- タイムライン出力時に固定列をまたいで日付見出しを結合しないよう修正。
- `.env.local` の GAS URL を再デプロイ後のWebアプリURLへ更新。
- タイムライン出力から長い説明文行を削除。
- 滞在時間0分の通過地点も、タイムライン上で最小1枠表示するよう修正。
- タイムライン生成時、保存済みの到着時刻・出発時刻を優先してUI表示とのズレを抑えるよう修正。
- `.env.local` の GAS URL を最新のWebアプリURLへ更新。
- GitHub Pages公開用に、ViteのbaseをGitHub Actions上のリポジトリ名から自動設定するよう修正。
- GitHub PagesへデプロイするGitHub Actions workflowを追加。
- READMEにGitHub Pages公開手順と必要なRepository secretsを追記。
- GitHub ActionsでSecretsとVariablesのどちらからでも本番環境変数を読めるよう修正し、必須値が未設定ならビルドを失敗させるチェックを追加。
- GitHub Pages公開用に、Google Maps APIキー・GAS URL・管理者PINをアプリ設定へ戻し、環境変数なしでも動作するよう修正。
- 京都・奈良限定モードの検索範囲を京都府・奈良県全体へ広げ、検索語に余計な「京都 奈良」を付加しないよう修正。
- タイムラインで滞在時間0分の通過地点が次の地点に上書きされないよう、同じ時刻枠では `通過地点 → 滞在地点` として併記するよう修正。
- GitHub Pages公開用のGAS URLを新しいWebアプリデプロイURLへ更新。
- タイムラインで移動時間も青い横棒として表示し、0分地点が次の滞在地点と混ざらないよう修正。
- GitHub Pages公開用のGAS URLを再デプロイ後の新しいWebアプリURLへ更新。
- 生徒ログインと管理者タイムラインの対象班を `01` から `14` へ拡張。
- GitHub Pages公開用のGAS URLを新しいWebアプリURLへ更新。
- 地点ごとの「地図に表示」設定を保存データに持たせ、再読み込み後も誤った地図登録を非表示にできるよう修正。
- GAS保存先シートに入力日時と更新日時を記録するよう修正。
- 行程表上で目的地の滞在時間を変更し、後続時刻を再計算できるよう修正。
- GitHub Pages公開用のGAS URLを新しいWebアプリURLへ更新。
- 行程表上で目的地までの移動時間と移動手段を変更し、後続時刻を再計算できるよう修正。
- 場所候補を選んだ時点で、地図に別色の番号付き仮マーカーを表示するよう修正。
